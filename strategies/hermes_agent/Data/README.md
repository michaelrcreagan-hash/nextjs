# Data Requirements

> Generated by /cbt:discover — see `../DISCOVERY.md` for the full spec.
> Place additional data files in this folder.

## Required Files

| File | Format | Required Columns | Notes |
|------|--------|------------------|-------|
| `btc_historical_data.csv` | CSV, daily | Date, BTC-USD, IBIT, FBTC, ARKB, BITB, ETHA, MSTR, ETH-USD, SOL-USD, SPY, TLT | Have — 525 rows, 2024-07-23 → present |
| `btc_ml_features.csv` | CSV, daily | Date + engineered features + `target` | Have — same window; inherits the 0.48-accuracy caveat, don't trust as a validated signal |
| `real_accounts/merrill_ira_positions.csv` | CSV, point-in-time | Description, Symbol, Quantity, Total Cost Basis, Estimated Market Value, Unrealized Gain/(Loss) | Have — 69 positions, ~$9,860 total, snapshot only (no history) |
| `real_accounts/coinbase_spot_holdings.csv` | CSV, point-in-time | symbol, quantity, market_value_usd, unrealized_gain_usd | Have — 20 tokens, ~$19,846 total, snapshot only; HBAR row has an ambiguous market-value field, flagged in-file |
| `real_accounts/hyperliquid_perps_positions.md` | Markdown, point-in-time | freeform, not yet normalized | Have but messy — two unclear tickers ("Spcx", "Mu"), no account-level equity figure |
| Daily OHLCV for the ~51 Merrill tickers not in `trading/hedgefund/data_long/` | CSV/Parquet, daily | timestamp/open/high/low/close/volume | **Need** — see DISCOVERY.md's universe table for the full list |
| Daily OHLCV for Coinbase's 18 non-BTC/ETH tokens (LINK, SOL, SUI, CAKE, UNI, ONDO, NEAR, AERO, HBAR, CFG, SYRUP, ARB, DOGE, ENA, KMNO, EIGEN, IP, MOVE) | CSV/Parquet, daily | timestamp/open/high/low/close/volume | **Need** |
| Fidelity options/leveraged-ETF universe not already cached (IBIT, TQQQ, JEPI, SLV, SPHB, CLPT, WULF, STRIVE) | CSV/Parquet, daily | timestamp/open/high/low/close/volume | **Need** |
| Hyperliquid account equity / margin balance | JSON or API pull | account-level, not per-position | **Need** — required before the perps/margin book can be sized in a backtest |

## Data Format Guidelines

### CSV Files
- First row should be headers
- Timestamps in ISO format (YYYY-MM-DD HH:MM:SS) or Unix milliseconds
- No trailing commas

### Parquet Files
- Preferred for large datasets
- Timestamps as datetime64

### Common Columns

| Column | Type | Description |
|--------|------|--------------|
| `timestamp` | datetime | Bar timestamp (open time) |
| `open` | float | Open price |
| `high` | float | High price |
| `low` | float | Low price |
| `close` | float | Close price |
| `volume` | float | Trading volume |

## Data Sources

- FRED (macro: CPI, T10Y2Y, UNRATE, DXY, VIX, Fed funds)
- FMP (fundamentals/earnings/catalysts/analyst sentiment/options)
- Alpaca (paper-sandbox portfolio/equities/options — not real capital)
- Coinbase (real spot holdings) + Hyperliquid + OKX (crypto perps: funding/OI/CVD)
- CoinGecko, Alpha Vantage
- Merrill IRA / Fidelity brokerage (manual CSV/transaction exports — real capital)
- `trading/hedgefund/data_long/` in this repo — already covers 18 of the 69 Merrill tickers plus BTC/ETH
- BTC ML feature set (`btc_ml_features.csv`, `btc_historical_data.csv`) — 0.48 directional-accuracy / 0.46 AUC caveat, unvalidated

## Real account structure (confirmed 2026-08-26)

| Account | Role | ~Value | Data status |
|---|---|---|---|
| Merrill IRA | Retirement | ~$9,860 | Snapshot in `real_accounts/merrill_ira_positions.csv` |
| Fidelity | Brokerage/margin/options | Unknown net equity | Transaction log only, in `bus_snapshots/` |
| Coinbase | Crypto spot | ~$19,846 | Snapshot in `real_accounts/coinbase_spot_holdings.csv` |
| Hyperliquid | Crypto perps/margin | Unknown | Per-position P&L only, in `real_accounts/hyperliquid_perps_positions.md` |
| Alpaca | Paper-trading sandbox | ~$16.4k (not real) | Out of scope for the real-capital goal |
| Prop firm | Separate challenge account | N/A | Out of scope for hermes_agent |

## Validation

After placing your data, the /cbt:build process will validate:
- [ ] All required files present
- [ ] No gaps in timestamps
- [ ] No missing values in critical columns
- [ ] Sufficient history for strategy requirements

---

*This file will be updated by /cbt:discover with specific requirements for your strategy.*
